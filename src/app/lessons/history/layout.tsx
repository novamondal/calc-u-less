export const metadata = {
	title: "History of Calculus",
	description: "Learn about the history of calculus",
};

export default function HistoryLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
