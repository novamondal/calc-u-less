export const metadata = {
	title: "Numbering Systems",
	description: "Learn about numbering systems other than base 10",
};

export default function NumberingLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
